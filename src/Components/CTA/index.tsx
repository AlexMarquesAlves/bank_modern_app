// imports
import styles from "../../Styles";
import { Button } from "../Button";

export const CTA = () => {
  return (
    <section>
      <div>
        <h2>Let's try our service now!</h2>
      </div>
    </section>
  );
};
